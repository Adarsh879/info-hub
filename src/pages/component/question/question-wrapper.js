import React from "react";

import "./question-wrapper.css";

const QuestionWrapper = ({ children }) => {
  return <div className="question-wrapper">{children}</div>;
};

export default QuestionWrapper;
